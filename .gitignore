# Personal working files generated by the old local workflow / by using this app —
# not part of the app itself, so they shouldn't end up in the public repo.
/ALL_LECTURES.md
/PCOL2021_Viewer.html
/pdf_ALL_LECTURES.pdf
*-viewer.html
pdf_*.pdf
temp_*.html

# OS junk
.DS_Store
Thumbs.db

# Local Claude Code settings (machine-specific, not project config)
.claude/
